"use strict";
exports.__esModule = true;
require("./store");
/*
const lightHub = new Yeelight();
(async ()=> {
  await lightHub.discover()
  const dev = lightHub.devices[0]
  await dev.connect()
  dev.socket.on("data", (da) => console.log(da.toString()))
})()
*/
